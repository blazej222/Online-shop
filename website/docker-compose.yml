services:
  mysql:
    platform: linux/x86_64
    container_name: mysql
    image: mariadb
    user: 1000:1000
    restart: unless-stopped
    environment:
      - MYSQL_ROOT_PASSWORD=root123
    ports:
      - 3306:3306
    volumes:
      - ./db_dump:/docker-entrypoint-initdb.d
      - ./database:/var/lib/mysql
    networks:
      - prestashop_net
  prestashop:
    platform: linux/x86_64
    container_name: prestashop
    image: prestashop/prestashop:1.7.8-apache
    #exec apache2-foreground
    command: /bin/sh -c "usermod -u 1000 www-data && exec apache2-foreground" # chown -R 1000:1000 /tmp/data-ps && /tmp/docker_run.sh
    restart: unless-stopped
    depends_on:
      - mysql
    ports:
      - 8080:80
    volumes:
      - ./src:/var/www/html
    networks:
      - prestashop_net
networks:
  prestashop_net: